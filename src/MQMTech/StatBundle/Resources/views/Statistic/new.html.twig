<h1>Statistic creation</h1>

<form action="{{ path('stats_crud_stat_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('stats_crud_stat') }}">
            Back to the list
        </a>
    </li>
</ul>
