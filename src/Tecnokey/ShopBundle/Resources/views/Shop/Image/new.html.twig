<h1>Shop\Image creation</h1>

<form action="{{ path('crud_image_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('crud_image') }}">
            Back to the list
        </a>
    </li>
</ul>
