<h1>Shop\ShoppingCart creation</h1>

<form action="{{ path('shop_shoppingcart_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('shop_shoppingcart') }}">
            Back to the list
        </a>
    </li>
</ul>
