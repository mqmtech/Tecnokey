<h1>Shop\Order creation</h1>

<form action="{{ path('crud_order_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('crud_order') }}">
            Back to the list
        </a>
    </li>
</ul>
